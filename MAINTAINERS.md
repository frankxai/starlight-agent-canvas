# Maintainers

Current maintainer:

- Frank / Starlight

## Maintainer Duties

- Keep the local-first and MCP safety boundaries intact.
- Review changes to schemas, store behavior, ingestion, MCP tools, install scripts, security posture, and public docs.
- Keep `README.md`, `docs/install.md`, `docs/operator-loop.md`, `docs/readiness-evidence.md`, and `docs/github-readiness.md` aligned with the actual product.
- Require visual QA evidence for UI changes.
- Require `pnpm doctor:json` and release audit health for public release work.

## CODEOWNERS

`.github/CODEOWNERS` is intentionally conservative until the public GitHub remote and maintainer handles are finalized. Update it with real GitHub usernames or teams before enforcing branch protection.

