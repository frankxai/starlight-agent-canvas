# Readiness Evidence Matrix

Last local verification: 2026-07-02.

This matrix connects the product promises in `docs/prd.md` and `docs/user-flows.md` to current evidence. It is intentionally practical: if a row cannot point to a command, screenshot, test, or file, it is not considered proven.

Latest local proof commit: use `git log --oneline -1`; this matrix tracks evidence by artifact instead of relying on a hand-maintained hash.

| Promise | Current Evidence | Status |
| --- | --- | --- |
| First screen is the usable workspace | `apps/web/components/WorkspaceClient.tsx`, `docs/scene-brief.md`, `docs/visual-qa/desktop-live-composer-modes.png`, `docs/visual-qa/mobile-live-composer-modes.png`; first-viewport `Paste / Drop Anything` composer plus desktop empty-canvas capture box | Proven locally |
| First screen shows the human operating loop | `operator-loop` in `apps/web/components/WorkspaceClient.tsx`, Playwright assertions in `apps/web/tests/workspace.spec.ts`, `docs/scene-brief.md`, `docs/user-flows.md`, `docs/visual-qa/desktop-operator-loop-active.png`, `docs/visual-qa/mobile-operator-loop-active.png`, `docs/visual-qa/desktop-operator-loop-reduced-motion.png` | Proven locally |
| First-use canvas is actionable | Blank first-run canvas, interactive empty-state `Map`/`Note`/`Upload`, canvas toolbar `Note`, right-rail empty inspector actions, and visual captures in `docs/visual-qa/desktop-source-receipt-actions.png` / `docs/visual-qa/mobile-source-receipt-actions.png` | Proven locally |
| Intake preview shows what will be mapped | `intake-preview` and `rail-intake-preview` in `apps/web/components/WorkspaceClient.tsx`; Playwright preview chip assertions | Proven locally |
| Intake map preview makes source mapping explicit | `intake-map-preview-summary` and `intake-map-preview` in `apps/web/components/WorkspaceClient.tsx`; Playwright assertions in `apps/web/tests/workspace.spec.ts`; visual captures in `docs/visual-qa/desktop-intake-map-preview-youtube.png`, `docs/visual-qa/mobile-intake-map-preview-youtube.png`, and `docs/visual-qa/desktop-intake-map-preview-youtube-reduced-motion.png` | Proven locally for showing future node kind, artifact kind, readiness state, source URL, and output-action mode before mapping |
| Latest intake receipt shows what was mapped | `context-mapping-receipt`, `context-receipt-items`, and receipt-scoped `Context`/`Codex` actions in `apps/web/components/WorkspaceClient.tsx`; Playwright assertions for video reference and summarize-output receipts | Proven locally for created node kind, artifact kind, Codex-ready state, inspect action, scoped context copy, scoped Codex copy, and output linkage |
| Intake trace persists for humans and MCP agents | `intakeTraces` in `packages/core/src/schemas.ts`, `packages/core/src/source-intake.ts`, `FileCanvasStore.appendIntakeTrace`, `intake-trace-panel` in `apps/web/components/WorkspaceClient.tsx`, `packages/core/src/exporters.ts`, MCP structured `trace`, `docs/visual-qa/desktop-intake-trace-panel.png`, `docs/visual-qa/mobile-intake-trace-panel.png` | Proven locally for paste/drop/upload traces, readiness labels, scoped Inspect/Context/Codex actions, MCP `get_canvas` visibility, selected JSON export filtering, and Context/Codex intake trace manifests that recompute selected trace summaries to keep unrelated material out |
| User can paste/drop links, images, transcripts, files, and notes | Playwright `apps/web/tests/workspace.spec.ts`; web routes under `apps/web/app/api/canvases/[id]/ingest`; `packages/core/src/__tests__/intake.test.ts`; focused Playwright desktop workflow passed on 2026-07-02 after PDF and clipboard-denied fallback coverage were added | Proven for text, URL+notes, image URL+notes, image file upload, PDF upload, YouTube manual transcript, mixed YouTube/video/image paste with attached transcript/notes/OCR grouping, Markdown file upload, paste-anywhere, `Paste & Map` affordance, blocked/empty clipboard manual fallback, and text drop-to-canvas happy paths |
| Mixed media paste keeps source-specific notes attached | `packages/core/src/intake.ts`; `packages/core/src/__tests__/intake.test.ts`; `packages/mcp/src/__tests__/tool-handlers.test.ts` | Proven locally for YouTube/manual transcript, generic video notes, and image OCR/visual notes in one paste blob, with no duplicate loose note node for the consumed media-specific text |
| YouTube is transcript-first with manual fallback | `packages/core/src/ingest.ts`, `docs/user-flows.md`, core ingest tests, Playwright YouTube manual transcript path | Proven locally |
| Non-YouTube video links can be captured as first-class video context | `apps/web/components/WorkspaceClient.tsx`, `apps/web/app/api/canvases/[id]/nodes/route.ts`, Playwright `source_video` assertion, `packages/mcp/src/tool-handlers.ts`, `docs/visual-qa/desktop-self-serve-video-intake.png`, `docs/visual-qa/desktop-self-serve-video-mapped.png` | Proven locally for `source_video` nodes, `video` artifacts, attached notes, chunks, and exported `media: video_reference`; provider transcription remains future work |
| Image links and screenshots can be captured as first-class visual context | `apps/web/components/WorkspaceClient.tsx`, `apps/web/app/api/canvases/[id]/nodes/route.ts`, `apps/web/app/api/canvases/[id]/ingest/image/route.ts`, Playwright `source_image` assertions, `packages/mcp/src/tool-handlers.ts`, `docs/visual-qa/desktop-image-intake.png`, `docs/visual-qa/mobile-image-intake.png` | Proven locally for `source_image` nodes, `image` artifacts, image preview, notes/chunks, `imageUrl`/`imageDataUrl` provenance, and export; OCR/provider vision remains future work |
| Reference-only sources can be enriched after mapping | `FileCanvasStore.enrichSourceNode`, `/api/canvases/[id]/nodes/[nodeId]/enrich`, inspector `source-enrichment` UI, MCP `enrich_source_node`, Playwright enrichment assertions, MCP handler tests and smoke script | Proven locally for turning a bare video reference into Codex-ready video notes with rebuilt chunks, source readiness, search/export visibility, and intake trace |
| Context gaps are visible before the user searches the graph | `context-gaps` lane in `apps/web/components/WorkspaceClient.tsx`, shared `describeSourceReadiness`, Playwright reference-only video assertions, `docs/visual-qa/desktop-context-gaps-reference.png`, `docs/visual-qa/mobile-context-gaps-reference.png`, `docs/visual-qa/desktop-context-gaps-reference-reduced-motion.png` | Proven locally for listing reference-only/needs-context sources and jumping to the enrichment textarea |
| Source readiness is visible to humans and MCP agents | `packages/core/src/readiness.ts`, `packages/core/src/__tests__/readiness.test.ts`, inspector `source-readiness` UI in `apps/web/components/WorkspaceClient.tsx`, Playwright `source-readiness-*` assertions, MCP `sourceReadiness` structured content in `packages/mcp/src/tool-handlers.ts` | Proven locally for Codex-ready transcript, video notes, visual notes, PDF text, URL references, and reference-only/needs-context states |
| First-viewport quick starters make intake self-serve | `canvas-quick-start`, `new-blank-canvas`, empty primary-action focus handling, and `context-loop` in `apps/web/components/WorkspaceClient.tsx`; Playwright desktop/mobile assertions; visual captures in `docs/visual-qa/desktop-first-touch-active-intake.png`, `docs/visual-qa/mobile-first-touch-active-intake.png`, `docs/visual-qa/desktop-self-serve-video-intake.png`, and `docs/visual-qa/mobile-self-serve-note-intake.png` | Proven locally |
| Workflow templates are guided operating maps | Rich template metadata and ordered workflow nodes in `packages/core/src/templates.ts`; `workflow-map` in `apps/web/components/WorkspaceClient.tsx`; core template tests and Playwright template-launch assertions | Proven locally for competitor teardown template launch, stage navigation, and context export |
| First-run activation is visible and machine-readable | `docs/activation.md`, `activation` payload in `apps/web/app/api/setup/status/route.ts`, `activation-runway` in `apps/web/components/WorkspaceClient.tsx`, and Playwright setup assertions | Proven locally for install health, proof canvas, source mapping, context export, Codex MCP command, first-run check command, and Codex activation prompt |
| New users can load a working proof canvas from the app | `apps/web/app/api/canvases/demo/route.ts`, `load-demo-canvas` controls in `apps/web/components/WorkspaceClient.tsx`, Playwright bundled demo assertion, `docs/visual-qa/desktop-demo-proof-canvas.png`, `docs/visual-qa/mobile-demo-proof-canvas.png` | Proven locally for in-app import of `examples/demo-canvas.json`, selected YouTube receipt, chunk preview, and context export |
| User can run local actions and inspect outputs | `packages/core/src/actions.ts`, `packages/core/src/__tests__/actions.test.ts`, Playwright `ask-canvas` path | Proven locally |
| JSON import/export is portable and non-destructive | `packages/core/src/store.ts`, `apps/web/app/api/canvases/import/route.ts`, `apps/web/components/WorkspaceClient.tsx`, `packages/core/src/__tests__/store.test.ts`, Playwright import preview/cancel/confirm path | Proven locally; imports show counts, node-kind preview, same-id conflict state, explicit cancel/confirm controls, duplicate imports preserve the existing canvas while creating a copy, and confirmed imports auto-focus source evidence for immediate inspection |
| Agent context packet is available | `packages/core/src/exporters.ts`, web `format=context` route, MCP `export_canvas` with `format: "context"`, live preview API check on 2026-07-02 | Proven locally with node index, chunk manifest, intake trace manifest, evidence corpus, and continuation prompt |
| Codex-ready handoff prompt is available | `packages/core/src/exporters.ts`, web `format=codex` route, `Codex` workspace button, CLI `--format codex`, MCP `export_canvas` with `format: "codex"`, Playwright/API and smoke assertions | Proven locally with embedded context packet and intake trace manifest |
| Selected evidence can be exported without unrelated canvas material | `scopeCanvasToNodes` and `describeCanvasExportScope` in `packages/core/src/exporters.ts`, API `nodeIds` query, CLI `--nodes`, MCP `export_canvas.nodeIds`, Playwright selected/full export assertions, core/MCP/CLI smoke assertions | Proven locally |
| Source chunks and citations are available | `packages/core/src/chunks.ts`, `packages/core/src/actions.ts`, `packages/core/src/__tests__/actions.test.ts`, `packages/core/src/__tests__/store.test.ts`, inspector citation UI in `apps/web/components/WorkspaceClient.tsx` | Proven at core/UI level |
| Citations navigate back to evidence | Inspector `citation-focus`, run-log `run-citation-focus`, focused chunk preview in `apps/web/components/WorkspaceClient.tsx`; Playwright citation focus assertions | Proven locally for selected-source ask citations and run-log citation chips |
| Selected sources expose a usable context receipt | `source-receipt`, `source-readiness`, `source-chunk-preview`, `selected-source-ask`, and `selected-source-copy` in `apps/web/components/WorkspaceClient.tsx`; Playwright assertions for YouTube/manual transcript readiness, PDF readiness, image readiness, video-note readiness, and selected-source ask | Proven locally |
| Search results navigate back to graph evidence | Web search UI in `apps/web/components/WorkspaceClient.tsx`; Playwright search-focus assertion | Proven locally |
| Selected context is visible before actions | `selected-context` tray in `apps/web/components/WorkspaceClient.tsx`; Playwright assertion | Proven locally |
| Codex export scope is visible before handoff | `codex-export-preview` in `apps/web/components/WorkspaceClient.tsx`, shared `describeCanvasExportScope` helper, Playwright assertions for canvas and selected modes, `packages/core/src/__tests__/exporters.test.ts`, `docs/visual-qa/desktop-add-anything-codex-preview.png`, `docs/visual-qa/mobile-codex-export-preview.png`, `docs/visual-qa/desktop-codex-export-preview-reduced-motion.png` | Proven locally |
| Handoff readiness is visible before export | `handoff-readiness` lane in `apps/web/components/WorkspaceClient.tsx`; Playwright assertions for evidence, synthesis, Codex state, and actions | Proven locally for live canvas/setup-derived readiness indicators |
| MCP can read/write canvas state safely | `packages/mcp/src/index.ts`, `packages/mcp/src/tool-handlers.ts`, `pnpm mcp:smoke` covers 19 tools, source ingest, source enrichment, source readiness, edges, search, import, and export | Proven locally |
| MCP can ingest text, URL, YouTube, generic video, image, and PDF sources | MCP tool schemas; handler tests; smoke test includes text, URL fallback, YouTube/manual transcript, generic video reference, source enrichment, image reference, PDF, connected nodes, chunk-aware search, context chunk manifest assertions, and durable trace assertions | Proven locally for text, URL fallback, YouTube/manual transcript, mixed-media `ingest_anything` grouping, generic video reference, source enrichment, image reference/upload handler coverage, PDF, source readiness, and persistent trace receipts; live URL quality remains best-effort |
| MCP and web support graph positioning | Web drag persistence and MCP optional `position` inputs; MCP handler test asserts position | Proven locally |
| Web/API can create explicit graph relationships | React Flow `onConnect`, toolbar `Connect`, `/api/canvases/[id]/edges`, `FileCanvasStore.connectNodes`, Playwright edge API assertion | Proven locally for safe non-destructive edge creation and exported graph relationships |
| New users have a one-command setup path | `scripts/setup.mjs`, `pnpm setup:local`, install docs, dry-run command verification | Proven locally |
| GitHub users have a non-interactive first-run proof | `scripts/first-run-check.mjs`, `pnpm first-run:check`, install docs, CI `first-run:check -- --skip-build` step | Verifies production preview boot, temporary data home, setup status, demo import, canvas listing, context export, mixed YouTube/video/image/web/text intake through `/api/canvases/:id/ingest/anything`, local action output, and clean server/data cleanup |
| First success is a maintained human plus Codex contract | `scripts/first-success-contract.mjs`, `pnpm first-success`, `pnpm first-success:json`, `docs/first-success.md`, `/api/setup/status.firstSuccess`, `first-success-contract` UI panel, release-audit JSON shape check, CI `first-success:json` step | Proven locally for install/open/capture/inspect/handoff/Codex phases, supported input contracts, proof commands, and Codex MCP loop |
| Supported inputs are visible in product and machine contracts | `docs/first-success.contract.json`, `/api/setup/status`, `input-contract-strip`, `first-success-input-contracts`, and Playwright setup assertions list YouTube, video, image, web, PDF, text, and note mappings | Proven locally by `pnpm first-success:json`, `pnpm typecheck`, `pnpm test`, `pnpm build`, and `pnpm test:e2e` |
| Terminal users can operate local canvases without browser or MCP restart | `scripts/canvas.mjs`, `scripts/canvas-smoke.mjs`, `pnpm canvas:smoke`, `docs/cli.md`, CI `canvas:smoke` step | Proven locally for demo import, list, search, context export, full Codex handoff export, and selected-node Codex handoff export against a throwaway local home |
| Local install readiness is machine-readable | `scripts/doctor.mjs`, `pnpm doctor:json`, `docs/operator-loop.md`, `docs/install.md`, CI `doctor:json` step | Proven locally for pass/warn/fail health output with repo root, data home, MCP CLI path, Codex config path, checks, and next steps |
| Adoption readiness is machine-readable and human-readable | `scripts/adoption-report.mjs`, `pnpm adoption:report`, `pnpm adoption:report:json`, `docs/adoption-report.md`, CI `adoption:report:json` step | Proven locally for combined doctor health, release audit health, demo proof, visual QA evidence, Git/GitHub metadata when available, Codex MCP path/home, first-success commands, and compact Codex prompt |
| GitHub/release readiness is machine-checkable | `scripts/release-audit.mjs`, `pnpm release:audit`, `docs/release-audit.md`, CI `release:audit` step | Proven locally for required OSS/community files, docs, examples, scripts, CI gates, demo proof, visual evidence, env hygiene, tracked/staged public files, and runtime-data safety |
| Public contributor governance exists | `CODE_OF_CONDUCT.md`, `SUPPORT.md`, `GOVERNANCE.md`, `MAINTAINERS.md`, `.github/CODEOWNERS`, `.github/ISSUE_TEMPLATE/config.yml`, `.github/pull_request_template.md` | Proven locally for project norms, support routing, governance boundaries, maintainer duties, CODEOWNERS placeholder, security-reporting contact link, and PR verification checklist |
| Codex MCP can be installed safely | `scripts/install-codex-mcp.mjs`, `pnpm mcp:install:codex`, timestamped backup before `--write`, dry-run verification, and `pnpm doctor` path/home checks | Proven locally |
| Setup and Codex status are visible in the app | `apps/web/app/api/setup/status/route.ts`, `Setup / MCP` panel in `apps/web/components/WorkspaceClient.tsx`, Playwright setup-panel assertion; server is considered wired only when config sections, CLI path, and canvas home match | Proven locally |
| Codex tool path is visible in the app | `agent` payload in `apps/web/app/api/setup/status/route.ts`, `agent-toolbelt` in `apps/web/components/WorkspaceClient.tsx`, Playwright setup assertions | Proven locally for visible `get_latest_canvas`, `ingest_anything`, `enrich_source_node`, `run_node_action`, `export_canvas`, adoption report command, terminal Codex handoff command, and agent prompt copy |
| Primary composer explains Codex-readable context | `live-intake-helper` in `apps/web/components/WorkspaceClient.tsx`, Playwright assertion in `apps/web/tests/workspace.spec.ts`; new visual evidence pending after this slice | Proven locally for persistent first-viewport copy on blank and existing canvases |
| Local data remains outside Git | `.gitignore`, `AGENT_CANVAS_HOME`, `packages/core/src/home.ts`, `pnpm doctor` | Proven by repo structure and doctor |
| Security scan is clean | `Invoke-RepoSecurityScan.ps1 -Path ...` passed on 2026-07-02 after generated cache cleanup | Proven locally |
| Public demo proves the product loop | `examples/demo-canvas.json`, `docs/demo-walkthrough.md`, `examples/mcp/README.md`, in-app `Demo` loader route; JSON parse check passed | Proven locally |
| Human and agent operating model is documented | `docs/operator-loop.md`, README docs index, MCP resource `starlight-agent-canvas://docs/operator-loop` | Proven locally |
| Visual QA passes 26/30+ | `docs/design-loop-evidence.json` validates and scores 28/30 | Proven locally |

## Current Known Gaps

- Non-YouTube video links are safe references plus notes in v0.1; provider-specific transcript adapters are not implemented yet, but manual source enrichment is first-class.
- Image links/uploads are first-class context references plus notes in v0.1; OCR and provider-backed image reasoning are not implemented yet, but manual OCR/visual-note enrichment is first-class.
- Mac/Linux install screenshots are not yet captured.
- Public GitHub remote is connected at `https://github.com/frankxai/starlight-agent-canvas.git`; hosted deployment/domain are still future release work.
- Hosted deployment, auth, collaboration, billing, marketplace, and provider-backed AI actions remain v0.2+ decisions.
